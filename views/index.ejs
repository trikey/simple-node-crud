<% include header %>
<p>Всего записей <%= total %></p>
<p>Записи нагло пизджатся из оперативки при помощи memcahced</p>
<ul>

    <% items.forEach(function(item) { %>
    <li>
        <a href="/<%= item.id %>"><%= item.title %></a>
    </li>
    <% }); %>
</ul>
<% include footer %>